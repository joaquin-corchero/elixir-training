defmodule GithubApi.Github.CodeRepos.RepositoryTests do
  use ExUnit.Case, async: false
  #alias GithubApi.Github.CodeRepos.Repository
  #alias GithubApi.Github.CodeRepos.CodeRepo

  #test "returns an empty list when there are no records" do
  #  assert Repository.all == []
  #end
end
